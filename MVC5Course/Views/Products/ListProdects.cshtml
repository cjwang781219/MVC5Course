@model IEnumerable<ProdectLiteVM>

@{
    ViewBag.Title = "ListProdects";
}

<h2>ListProdects</h2>
<h1>@ViewBag.test1</h1>
<h1>@ViewData["test2"]</h1>
@if (TempData["successMsg"] != null)
{
    <h1>@TempData["successMsg"]</h1>
}

<form method="get">
    名稱
    @Html.TextBox("q")
    stock範圍

    <div>
        @Html.TextBox("Stock_s", null, new { size = "10", type = "number" })
        @Html.ValidationMessage("Stock_s")
    </div>
    ~
    <div>
        @Html.TextBox("Stock_e", null, new { size = "10", type = "number" })@Html.ValidationMessage("Stock_s")
    </div>
    <button>Searc</button>
</form>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{ 
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stock)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Stock)
                </td>
            </tr>
        }

    </table>
    }